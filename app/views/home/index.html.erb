<!-- Banner -->
<section id="banner">
	<h2> Is your computer shitty?</h2>
	<p>Find a computer and start coding</p>
	<ul class="actions">
		<li><a href="#" class="button special">Learn more</a></li>
	</ul>
</section>
 <%= link_to 'home', controller: 'home' %> 
<table>
  <tr>
    <th>Name</th>
  </tr>
  
  <% @buildings.each do |building| %>
    <tr>
      <td><%= building.name %></td>
      <td> <%= link_to 'Show lab rooms', building_path(building.id) %></td>
    </tr>
  <% end %>
</table>
